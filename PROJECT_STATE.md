# Project State: Karaoke Studio 2

This project is a clone of the `karaoke-video-creator` repository, specifically from the `autolyrics` branch.

## Purpose

This directory (`/Users/santosh/development/karaokestudio2`) was created to serve as a dedicated environment for new development on the `autolyrics` branch, keeping it isolated from the `main` branch and the original project directory.

## Current Status

- **Branch:** `autolyrics`
- **Remote:** `https://github.com/santoshk1970/karaoke-video-creator.git`
- **Last Major Change (before clone):**
    - Refactored `src/imageGenerator.ts` to support persistent speaker colors.
    - Updated `src/processor.ts` to manage and pass the active speaker color.
    - Committed and pushed all changes to the remote `autolyrics` branch.

The working directory is clean and up-to-date with the remote `autolyrics` branch.
